# 001 — 任务

## 骨架（本轮）

- [x] 写 `specs/README.md` 和本目录三份 SDD 文档
- [x] `client-data/README.md`：说明 drop 约定；不改现有图纸文件
- [x] `data/README.md` + `catalog/`、`pipelines/` 占位
- [x] `ontology/README.md`：只放类型定义
- [x] `.gitignore` 排除客户原文和 `data/datasets/`
- [x] `AGENTS.md`、`README.md` 的目录表补上四层和 `specs/`

## 明确不做

- 不写 DWG 解析管线
- 不写 catalog 实例 YAML
- 不定对象类型（`Drawing` / `Transformer` 等另开 spec）
