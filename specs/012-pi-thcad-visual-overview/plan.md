# 实现计划

1. 复用 capability 02 frame artifact，建立活动图与 DBMOD 一致性门禁。
2. 用独立 PowerShell COM 动作控制 THCAD 视窗和 `PNGOUT`，并实现现场恢复与 PNG 内容校验。
3. 在 Pi package 中实现固定模型、无工具的 fresh 视觉 child 和严格 JSON 契约。
4. 将视觉图片、assessment、父/子会话和 01–20 代次接入既有 review store。
5. 用 fixture、类型检查、真机出图、真实模型调用和 bundle verify 完成验收。
