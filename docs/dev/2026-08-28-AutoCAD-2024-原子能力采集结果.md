# AutoCAD 2024 原子能力采集结果

采集日期：2026-08-28。数字全部来自本机 `autocad-2024.*` 的 `inventory-manifest.json`、`capability-atoms.jsonl`、`enrichments/part-*.jsonl`，以及 `catalog_queue.py validate` 的现场输出。不含 THCAD 库存合计，也不含 curated 数据集合计。

本轮只生成 CapabilityAtom 与独立 enrichment。未生成 SemanticCapability、关系、curated、PostgreSQL、后端或前端。

## 结论

五个隔离库存位于 `data/datasets/staging/cad-capabilities/`：

| inventory | manifest.atoms | JSONL 行 | enrichment 行 | validate pending | --require-complete |
| --- | ---: | ---: | ---: | ---: | --- |
| autocad-2024.com | 19836 | 19836 | 19836 classified | 0 | 通过 |
| autocad-2024.dotnet | 26778 | 26778 | 26778 classified | 0 | 通过 |
| autocad-2024.lisp | 5226 | 5226 | 1077 classified + 4149 deferred | 0 | 通过 |
| autocad-2024.command | 9196 | 9196 | 9059 classified + 137 deferred | 0 | 通过 |
| autocad-2024.native | 193464 | 193464 | 0（允许 pending） | 193464 | 未要求；pending = raw_total |

每条原子 `observed_host_ids` 均为 `["autocad-2024"]`。四个可分类库存 `semantic_candidates` 非空条数均为 0，`runtime_probe` 证据均为 0。

## 宿主与安装证据

来源：`autocad-2024-host.json`（进程路径 + 文件版本 + 注册表 + COM/ROT）。

| 项 | 实测 |
| --- | --- |
| 观测宿主 | `autocad-2024` |
| 产品 | AutoCAD 2024（注册表 ProductNameGlob；文件版本 ProductName=AutoCAD；COM caption=`Autodesk AutoCAD 2024 - [开始]`） |
| 完整版本 | `R24.3.61.0.0`；COM 运行时 `24.3s (LMS Tech)` |
| 位数 | x64 |
| 安装根 | `D:\autocad2024\AutoCAD 2024` |
| 进程 | pid 19048，`D:\autocad2024\AutoCAD 2024\acad.exe`，`/product ACAD /language "zh-CN"` |
| 注册表 | `HKLM:\SOFTWARE\Autodesk\AutoCAD\R24.3\ACAD-7101:804`，Release `24.3.61.0`，LangAbbrev `chs` |
| COM | `AutoCAD.Application.24`，服务器即上述 `acad.exe` |
| 预检 caption | `Autodesk AutoCAD 2024 - [开始]`（开始页） |
| LISP/Command 附着时文档 | `Drawing1.dwg`（来自已捕获的 `lisp-runtime-probe.log`；探针 `Documents.Add()`） |
| 已加载模块 | `autocad-2024-host.json` 的 `loaded_modules`，均在安装根下 |
| 会话变量 | `LISPENABLED=nil`，`SECURELOAD=0` |

现场复验 `catalog_queue.py validate`（COM/.NET/LISP/Command 带 `--require-complete`，Native 不带）全部 exit 0，输出与上表 pending 一致。

---

## autocad-2024.com

来源：`autocad-2024.com/inventory-manifest.json`，atoms SHA-256 `c95b0f443216dfeb8bd80486d8e2eb82ad19a0cda6c05eaa85ac893c527946dd`。

**扫描边界**：`ExportAutoCad2024ComCapabilityAtoms.ps1` → TlbImp + `ComCapabilityScan.cs` + `com_atoms.py`。类型库来自 Autodesk Shared 的 `axdb24chs.tlb` / `AcAuthEntities24chs.tlb` / `acax24chs.tlb` 与安装根 `vl16_u.tlb`。ProgID 仅 `AutoCAD.*`。

**来源构件 SHA-256**（manifest `source_artifacts`）：

| artifact_id | name | version | sha256 |
| --- | --- | --- | --- |
| registry:hkcr-progid | HKCR ProgID (AutoCAD.*) | — | — |
| typelib:AcAuthEntities | AcAuthEntities | 24.3.61.0.0 | `7badfeca6dd6fe32611dd15cfaca6e2ea332c146992bfb0c4b805a9ad3adb955` |
| typelib:AutoCAD | AutoCAD | 24.3.61.0.0 | `b7a25740106adac600e43570675aa9cac8a971768792b0365a2facb653a77872` |
| typelib:AutoCADAxDb | AutoCADAxDb | 24.3.61.0.0 | `37ca7eb74db3d191f71b026ede1c1abbb5721984ebc0b048b621b090d8edceee` |
| typelib:VLAX | VLAX | — | `d2d9de463ff3560f759a31e240191767d5ddda62f6d16e7bd2b71c0f6db46457` |

**声明级计数**：interfaces 658，declared_methods 5297，declared_properties 8845，declared_events 54，progids 7，conversion_errors 0，reflection_errors 0。

**by_kind**：method 5297，property_get 8815，property_set 5609，event_subscribe 54，event_unsubscribe 54，progid_activation 7。合计 19836。

**分类**（199 个 enrichment part，19836 行）：classified 19836，deferred 0，failed 0，pending 0。`semantic_candidates` 非空 0。`operation_kinds`（可多值）：read 9864，edit 6106，invoke 5297，transform 1231，delete 518，create 404，event 216，select 16，import 13，lifecycle 10，save 8，export 4，display 2。

**两次导出**：emit1 = emit2 = final = `c95b0f443216dfeb8bd80486d8e2eb82ad19a0cda6c05eaa85ac893c527946dd`（registry 构件名按宿主参数化后冻结重导）。

**未覆盖**：未注册类型库、32 位 COM、非 `AutoCAD.*` ProgID。未在当前 DWG 上调用写入入口。

**复现**：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File dev-test/visualstudionetframework/probes/autocad-2024/ExportAutoCad2024ComCapabilityAtoms.ps1
uv run --project backend python data/pipelines/cad_capabilities/catalog_queue.py validate --manifest data/datasets/staging/cad-capabilities/autocad-2024.com/inventory-manifest.json --atoms data/datasets/staging/cad-capabilities/autocad-2024.com/capability-atoms.jsonl --enrichments data/datasets/staging/cad-capabilities/autocad-2024.com/enrichments --require-complete
```

产物：`data/datasets/staging/cad-capabilities/autocad-2024.com/{inventory-manifest.json,capability-atoms.jsonl,com-capability-scan.json,enrichments/}`。

---

## autocad-2024.dotnet

来源：`autocad-2024.dotnet/inventory-manifest.json`，atoms SHA-256 `11d12df55d4fb3f634e6eb48f7d35412c974ce2de6db2ee718bb7d85b07d8997`。

**扫描边界**：`ExportAutoCad2024DotNetCapabilityAtoms.ps1` → `ReflectionOnlyLoadFrom` + `DotNetCapabilityScan.cs` + `dotnet_atoms.py`。安装根内实测到的 9 个公开托管程序集。

**来源构件 SHA-256**：

| name | version | sha256 |
| --- | --- | --- |
| AcCui | 24.3.0.0 | `d68777e8bfa131527977b48346abd99106d7f18c02b6797f6beae37a721f041a` |
| AcMPolygonMGD | 16.0.0.0 | `045808da98d62dddc14f41aa857bc6a25258b6075721447fe0e0d0f4a748c50e` |
| AcTcMgd | 24.3.0.0 | `f1a445c1f7334fcd15d7aac08e6457b11493452d1adfa742f4eacbbe18dd7b4b` |
| Acdbmgd | 24.3.0.0 | `fc48a2d62c133d9ade32acc99ef14588bfa9286d7cf2cecba8be82a00506bf4a` |
| Acmgd | 24.3.0.0 | `3f495f7afd430306925ebc10d7f706108fd0ec54630571db8e6c01cd63d3ba2c` |
| AdUIMgd | 24.3.0.0 | `bb3e5bed48e8deb1a807bec669cdaa71a32e4c25b7660cb2d3cf7055653fb06d` |
| AdWindows | 5.0.1.2 | `fc115b3d4b99b8b129297bb0db44eb90df972821e9611f1a81e5520a0e67a17f` |
| accoremgd | 24.3.0.0 | `2b613b1ec6e68300e71b17d3d50702992f8460484cea17c03214ce4c9d2b4434` |
| acdbmgdbrep | 24.3.0.0 | `937802421df73ee9ea926fca95302d446804396154dec42020fc7e5a7e9cebb2` |

**声明级计数**：public_types 2790，declared_method_overloads 8832，declared_properties 8035，declared_events 536，declared_constructors 2014，declared_fields 6740，enum_literals 4635（不作为 Action），conversion_errors 0，reflection_errors 0。

**by_kind**：constructor 2014，method 8832，property_get 8015，property_set 4399，field_read 1515，field_write 931，event_subscribe 536，event_unsubscribe 536。合计 26778。

**分类**（268 个 enrichment part，26778 行）：classified 26778，deferred 0，failed 0，pending 0。`semantic_candidates` 非空 0，`runtime_probe` 0。`operation_kinds`：read 11423，invoke 8832，edit 6262，create 2674，lifecycle 2019，compute 1847，event 1072，transform 120，delete 65，save 51，select 23，import 12。

现场 `validate --require-complete`：`pending=0`。

**两次导出**：emit1 = emit2 = final = `11d12df55d4fb3f634e6eb48f7d35412c974ce2de6db2ee718bb7d85b07d8997`。

**未覆盖**：安装根内其它托管 DLL、私有类型、非公开成员、枚举常量 Action 化、P/Invoke。AutoCAD 宿主不附加 Teigha `ExplodeGeometry` runtime evidence。

**复现**：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File dev-test/visualstudionetframework/probes/autocad-2024/ExportAutoCad2024DotNetCapabilityAtoms.ps1
uv run --project backend python data/pipelines/cad_capabilities/catalog_queue.py validate --manifest data/datasets/staging/cad-capabilities/autocad-2024.dotnet/inventory-manifest.json --atoms data/datasets/staging/cad-capabilities/autocad-2024.dotnet/capability-atoms.jsonl --enrichments data/datasets/staging/cad-capabilities/autocad-2024.dotnet/enrichments --require-complete
```

产物：`data/datasets/staging/cad-capabilities/autocad-2024.dotnet/{inventory-manifest.json,capability-atoms.jsonl,dotnet-capability-scan.json,enrichments/}`。

---

## autocad-2024.lisp

来源：`autocad-2024.lisp/inventory-manifest.json`，atoms SHA-256 `0b4c3a66cd2d67150ec09ddd5956fe37390069f3479ba4d57f1bf3181b393553`。

**扫描边界**：`ExportAutoCad2024LispCommandCapabilityAtoms.ps1` 只附着 `AutoCAD.Application.24`。LSP 树：安装根 `Support` / `Express` / `Sample` / `Tutorial` / `UserDataCache` 与 HKCU 漫游 Support。运行时 `(atoms-family)` 只枚举，不调用。

**来源**：149 个 `.lsp`（逐文件 SHA-256 见 manifest）+ `session:autocad-2024`（name=AutoCAD，version=`24.3s (LMS Tech)`，sha256 `7ec87937f33ece738c88313c5aa8e8f5642b7f81c297a0247177ec47687d86ac`）。共享扫描：`autocad-2024.lisp-command-scan.json`、`autocad-2024.lisp-command-runtime.json`。

**声明级计数**：lsp_files 149，source_defuns 1278，source_lisp_functions 1077，runtime_symbols 4286，runtime_lisp_symbols 4149，loaded_arx 34，loaded_vlx 0。

**by_kind**：lisp_function 5226。

**分类**（53 个 part，5226 行）：classified 1077（`invoke`），deferred 4149（仅运行时符号名、无参数协议，`unknown`），failed 0，pending 0。`semantic_candidates` 非空 0。

**两次导出**：emit1 = emit2 = final = `0b4c3a66cd2d67150ec09ddd5956fe37390069f3479ba4d57f1bf3181b393553`。

**未覆盖**：未列入上述树的用户 LSP、未加载 VLX、`LISPENABLED=nil` 时不可达扩展、符号实参调用。

**复现**：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File dev-test/visualstudionetframework/probes/autocad-2024/ExportAutoCad2024LispCommandCapabilityAtoms.ps1
uv run --project backend python data/pipelines/cad_capabilities/catalog_queue.py validate --manifest data/datasets/staging/cad-capabilities/autocad-2024.lisp/inventory-manifest.json --atoms data/datasets/staging/cad-capabilities/autocad-2024.lisp/capability-atoms.jsonl --enrichments data/datasets/staging/cad-capabilities/autocad-2024.lisp/enrichments --require-complete
```

---

## autocad-2024.command

来源：`autocad-2024.command/inventory-manifest.json`，atoms SHA-256 `4e0476364a8809d9700af2c850c695973e95d9629a98977afccd84c87c44ab90`。

**扫描边界**：与 LISP 同一冻结会话。CUIX 按 zip 解析。

**来源**：15 个 CUIX（SHA-256 见 manifest）+ 相关 LSP + 同一 `session:autocad-2024`。`cui_parse_errors=0`。

**声明级计数**：cui_files 15，menu_macros 6907，command_tokens_unique 666，command_token_file_pairs 1951，source_commands 201，runtime_commands 137。

**by_kind**：command 2289，macro 6907。合计 9196。

**分类**（92 个 part，9196 行）：classified 9059（`invoke`），deferred 137（仅运行时命令名，`unknown`），failed 0，pending 0。`semantic_candidates` 非空 0。宏只作为编排入口，不把宏串综合效果归到单个 command 原子。

**两次导出**：emit1 = emit2 = final = `4e0476364a8809d9700af2c850c695973e95d9629a98977afccd84c87c44ab90`。

**未覆盖**：未加载企业 CUI、未进入上述树的用户菜单、命令真实执行效果。

**复现**：与 LISP 同一导出脚本；validate 把路径换成 `autocad-2024.command` 并加 `--require-complete`。

---

## autocad-2024.native

来源：`autocad-2024.native/inventory-manifest.json`，atoms SHA-256 `cab044644f04987c6133f67321754e6117e5e489830993b29703c5e04af56407`。

**扫描边界**：`ExportAutoCad2024NativeCapabilityAtoms.ps1` → 文件列表 `WriteInventoryScanFromFiles`。安装根 `*.arx`/`*.crx`/`*.dbx`、固定宿主 PE、当前已加载且落在白名单的模块。未知签名保持未知，不生成 P/Invoke。

**来源**：306 个 PE（plugin_modules 242 + host_modules 64），逐模块 SHA-256 在 manifest `source_artifacts`。扫描：`native-capability-scan.json`。

**声明级计数**：plugin_exports 98389，host_exports 95075，named_exports 131087，ordinal_only_exports 62377，decorated_exports 127508，standard_entries 568，sdk_headers 0，lib_files 0，coff_import_libs 0，pe_read_errors 0。

**by_kind**：native_export 193464。

**分类**：enrichment 文件 0、行 0。validate：classified 0，deferred 0，failed 0，pending 193464，unaccounted 0。pending = raw_total，全部计入 manifest。

**两次导出**：emit1 = emit2 = final = `cab044644f04987c6133f67321754e6117e5e489830993b29703c5e04af56407`。

**未覆盖**：公开 SDK 头/导入库（计数为 0）、白名单外第三方插件、推断 C++ 签名。

**复现**：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File dev-test/visualstudionetframework/probes/autocad-2024/ExportAutoCad2024NativeCapabilityAtoms.ps1
uv run --project backend python data/pipelines/cad_capabilities/catalog_queue.py validate --manifest data/datasets/staging/cad-capabilities/autocad-2024.native/inventory-manifest.json --atoms data/datasets/staging/cad-capabilities/autocad-2024.native/capability-atoms.jsonl --enrichments data/datasets/staging/cad-capabilities/autocad-2024.native/enrichments
```

Native 不加 `--require-complete`。

---

## 测试与 THCAD 保护（只读核对，不是 AutoCAD 合计）

新增：`data/pipelines/cad_capabilities/tests/test_autocad_isolation.py`，覆盖输出拒绝 `thcad-v24.*`、AutoCAD 原子不带 THCAD runtime evidence、`semantic_candidates=[]`、CUIX zip。

全套：`uv run --project backend python -m unittest discover -s data/pipelines/cad_capabilities/tests -v` 连续两次 65 tests OK。

开跑前记录的五个 `thcad-v24.{com,dotnet,lisp,command,native}` `inventory-manifest.json` / `capability-atoms.jsonl` SHA-256，结束时逐文件一致（manifest 字节与 atoms 字节也一致）。这些哈希只用于证明本轮未改写 THCAD 原始库存，不能当成 AutoCAD 原子数。

`thcad-v24.*` 原始 JSONL 的 LastWriteTime 均早于对应 `autocad-2024.*` 写出时间。不存在 `autocad-2024.semantic`。本 Goal 未执行 `semantic_relations.py`、`build_curated.py`，未写入 `backend/`、`frontend/`、`ontology/`、`harness/`。

工作区里既有的 backend/frontend 脏文件属于采集开始前已存在的其它改动，本轮采集脚本与 enrichment 没有改那些路径。

## 停止边界

未做跨宿主对比、关系、curated 扩写、入库、API、前端、在用户 DWG 上调用副作用入口、Native P/Invoke。后续对比必须另开规格，且不得回写两边原始原子。
