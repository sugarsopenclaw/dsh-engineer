recipe_id: generic.audit_duplicate_labels
version: "1.0.0"
summary: Deterministically report repeated normalized attached labels.
task_type: AUDIT
target_parameterized: true
supported_mutation_policies: [READ_ONLY]
quantity_bases: []
required_capabilities: [semantic.query]
required_rules: [generic.distinct_labels]
nodes:
  - {node_id: resolve_scope, block_id: scope.resolve, depends_on: []}
  - {node_id: audit, block_id: audit.duplicate_labels, depends_on: [resolve_scope]}
  - {node_id: decide, block_id: result.decide, depends_on: [audit]}
  - {node_id: act, block_id: artifact.read_only, depends_on: [decide]}
  - {node_id: validate, block_id: result.validate, depends_on: [act]}

