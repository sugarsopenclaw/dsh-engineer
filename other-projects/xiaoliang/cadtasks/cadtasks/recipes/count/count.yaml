recipe_id: generic.count_by_basis
version: "1.0.0"
summary: Count a parameterized semantic class without mixing identity bases.
task_type: COUNT
target_parameterized: true
supported_mutation_policies: [READ_ONLY]
quantity_bases: [DRAWING_OCCURRENCE, UNIQUE_TAG, BOM_DECLARED, PHYSICAL_INSTANCE]
required_capabilities: [semantic.query]
required_rules: []
nodes:
  - {node_id: resolve_scope, block_id: scope.resolve, depends_on: []}
  - {node_id: count, block_id: count.by_basis, depends_on: [resolve_scope]}
  - {node_id: decide, block_id: result.decide, depends_on: [count]}
  - {node_id: act, block_id: artifact.read_only, depends_on: [decide]}
  - {node_id: validate, block_id: result.validate, depends_on: [act]}

