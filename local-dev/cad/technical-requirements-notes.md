# 技术要求提取观察

这里保留实际图纸中的观察和 05 实现后的验证结果，不把七张图规律写成所有图纸都必须满足的硬规则。

- 沈变图纸的“技术要求”基本位于图框右上角。
- 图幅分区中，右侧的 `A` 和顶部靠右的 `16` 可以帮助确认图框的右上方向；这不表示标题必须严格落在 `A16` 单格内。
- 当前截图中标题更靠 `A14` 一带，整块技术要求横跨约 `13–16`，并从 `A` 向下延伸到 `B`、`C`。
- 后续验证时可以优先在图框右上区域寻找“技术要求”，但仍需保留图框内全局查找，避免“基本如此”被误写成绝对条件。
- 03 分区检测实测：七张图中有六张包含“技术要求”标题，全部位于 `A` 行；具体落点为三个 `A14`、两个 `A15`、一个 `A8`。这支持“右上区域优先”，也再次说明不能硬编码为 `A16`。
- 05 实测共整理出 6 段、54 条编号正文，其中 5 个无编号续行需要并回上一条。
- 标题既有左对齐也有居中对齐，因此识别使用文字包围盒和相对位置，不依赖 `horizontal_mode`。
- `5TBC.384.A110050.1_1` 的编号为 1–5、7–13；提取照原号保留并报告缺 6，不把连续性当作成功前提。
- 面向 LLM 的 Markdown 只放整理正文和编号检查；句柄、坐标、原始分行等证据保留在 JSON。
